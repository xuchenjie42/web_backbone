'use strict';

var run = require('../index');

run({ name: 'ls', args: ['-l'] }).then(function() {
  console.log(arguments);
});

// run({ name: 'ls', args: ['-l'] }, function() {
//   console.log(arguments);
// });