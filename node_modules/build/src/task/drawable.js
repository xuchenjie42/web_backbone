'use strict';

module.exports = require('../task').extend({
  name: 'drawable',
  execute: function() {
    this.gulp.src(this.src)
      .pipe(this.plugins.cached('drawable'))
      .pipe(this.plugins.remember('drawable'))
      .pipe(this.plugins.plumber())
      .pipe(this.gulp.dest(this.dst));
  }
}, { export: 'Drawable' });