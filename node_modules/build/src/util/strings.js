'use strict';

module.exports = {
  toProperCase: function(s) {
    return s && s.split(/\s+/).map(function(s) {
      return (s && s.length && s[0].toUpperCase() + s.substr(1)) || s;
    }).join(' ') || s;
  }
};