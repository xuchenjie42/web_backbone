'use strict';

var paths = require('./src/paths');

module.exports = {
  paths: paths,
  util: {
    run: require('run')
  },
  Scheme: require(paths.self.src().append('scheme').rel()),
  Task: require(paths.self.src().append('task').rel()),
  tasks: {
    drawable: require(paths.self.task().append('drawable').rel()),
    help: require(paths.self.task().append('help').rel()),
    js: require(paths.self.task().append('js').rel()),
    style: require(paths.self.task().append('style').rel()),
    sync: require(paths.self.task().append('sync').rel())
  },
  new: require(paths.self.src().append('gulp').rel()),
  setup: function() {
    require('./setup');
  }
};